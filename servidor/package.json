{
  "scripts": {
    "start": "json-server --watch db.json --static ../Modulos/dist --host 0.0.0.0 --port 3001"
    
  },
  "dependencies": {
    "json-server": "^0.17.4",
    "serve": "^14.2.6"
  }
}
